import{bootstrapSession,get,post}from"./api.js";import{renderHome}from"./views/home.js";import{renderAssistant}from"./views/assistant.js";import{renderCalendar}from"./views/calendar.js";import{renderNotifications}from"./views/notifications.js";import{renderDeferred,renderReview,renderSettings}from"./views/foundations.js";
const root=document.querySelector("#content"),title=document.querySelector("#page-title"),fresh=document.querySelector("#freshness");let briefing;
async function show(view,question=""){document.querySelectorAll("[data-view]").forEach(button=>button.classList.toggle("active",button.dataset.view===view));title.textContent=view[0].toUpperCase()+view.slice(1);history.replaceState({},"",`/app/${view}`);try{if(view==="home"){briefing=await get("/api/v1/dashboard/briefing");fresh.textContent=briefing.freshness.canvas_status;fresh.className="status "+briefing.freshness.canvas_status;renderHome(root,briefing,value=>show("assistant",value))}else if(view==="assistant")renderAssistant(root,question);else if(view==="calendar")await renderCalendar(root);else if(view==="notifications")await renderNotifications(root);else if(view==="review")await renderReview(root);else if(view==="settings")await renderSettings(root);else renderDeferred(root,view)}catch(error){root.textContent=`Unable to load this view: ${error.message}`}}
await bootstrapSession();document.querySelectorAll("[data-view]").forEach(button=>button.onclick=()=>show(button.dataset.view));document.querySelector("#logout").onclick=async()=>{await post("/api/v1/auth/logout",{});location.replace("/login")};const path=location.pathname.split("/")[2];show(path||"home");
