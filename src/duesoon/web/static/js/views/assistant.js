import{post}from"../api.js";import{node}from"./home.js";
export function renderAssistant(root,initial=""){root.replaceChildren();const log=node("div"),form=node("form","","assistant-box"),input=document.createElement("input"),button=node("button","Ask");input.maxLength=500;input.placeholder="What is due next?";form.append(input,button);root.append(log,form);async function ask(question){log.append(node("div",question,"message user"));button.disabled=true;try{const value=await post("/api/v1/dashboard/assistant",{question});const reply=node("div","","message");reply.append(node("strong",value.answer),node("p",`${value.mode} · ${value.confidence} · data ${value.data_freshness}`,"muted"));const evidence=node("div","","evidence");for(const item of value.evidence){const link=node("a",item.label);link.href=item.href;link.rel="noopener noreferrer";evidence.append(link)}reply.append(evidence);log.append(reply)}catch(error){log.append(node("div",error.message,"message error"))}finally{button.disabled=false}}form.addEventListener("submit",event=>{event.preventDefault();const value=input.value.trim();input.value="";if(value)ask(value)});if(initial)ask(initial)}
